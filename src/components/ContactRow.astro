---
import { site } from '../lib/data';
---

<ul class="meta-row">
  <li class="meta-location">{site.location}</li>
  {site.links.map((link) => (
    <li class="meta-link"><a href={link.url}>{link.label}</a></li>
  ))}
</ul>
